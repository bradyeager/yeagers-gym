# Vagaro Webhook Receiver — Yeager's Gym

Tiny Vercel serverless function that catches Vagaro events
(appointment/customer/transaction) and feeds the billing bot.

PHASE 1: LOGGING MODE — logs full headers + raw body, returns 200.
