# --- Secrets: never commit ---
.env
.env.*
*.key
*-credentials.json
secrets.json

# --- Local audit / working artifacts (not part of the website) ---
_audit/
.playwright-mcp/
audit-*.jpeg
audit-*.png

# --- Git worktrees (local only — a committed gitlink here stalls Pages builds) ---
.claude/worktrees/

# --- OS / editor noise ---
.DS_Store
Thumbs.db
